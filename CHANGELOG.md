# What's Changed

<!-- Keep only the latest release notes here. Do not include a version number or
append older releases. Replace the entries below when publishing a new release. -->

- Add a Kotlin Multiplatform implementation of R8 retrace for JVM and Wasm.
- Add the zero-dependency `retrace-wasm` npm package with TypeScript declarations
  and source maps.
- Add desktop and browser clients, including local support for large mapping files
  and reusable mapping indexes in the browser.
